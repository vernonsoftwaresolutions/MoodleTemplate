---
machine:
  python:
    version: "2.7.11"
  ruby:
    version: "2.4.0"

dependencies:
  override:
    - "aws configure set default.region us-west-2"
    - "aws configure set default.output json"

test:
  post:
    - "aws cloudformation validate-template --template-body file://moodletemplate.yml"
    
deployment:
  development:
    branch: master
    commands:
          - "aws s3 cp config.php s3://moodle-deployables" 
          - "aws cloudformation package --template-file moodletemplate.yaml --output-template-file moodletemplate-output.yaml --s3-bucket moodle-deployables"
          - "aws cloudformation deploy --template-file moodletemplate-output.yaml --capabilities CAPABILITY_IAM --stack-name moodletemplate --parameter-overrides Tenant=CompanyA DBInstance=moodle DBName=moodle DBIsMultiZone=false KeyName=SANDBOX CertBucket=moodle-deployables"
          - "aws cloudformation delete-stack --stack-name moodletemplate"
